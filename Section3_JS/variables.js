/**
 * When you use 'var' the varible is initialize at the start of the file, no matter where you define it.
 * When you use 'let' the variable is initialize where it is placed.
 * 
 */

console.log(miVariable)
var miVariable = 'variable'

let variable  = 'Variable defined using let '
console.log(variable)

const constant = 'This is a constant'
    //constant = 'trying to redefine a constant'
console.log(constant)
